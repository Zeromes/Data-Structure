作业一

一、数据结构题集P9： 1.9

二、计算n！的递归函数fac(n)如下，试分析它的时间复杂度T（n）。
   
    float fac(int n)
     { if(n<=1)fac=1；
       else fac=n*fac(n-1)；
      }

三、选择题

1、线性表采用链表存储时，其存储地址（D）
A）必须是连续的
B）部分地址必须是连续的
C）一定是连续的
D）连续不连续都可以

2、线性表是具有n个（C）的有限序列
A）表元素
B）字符
C）数据元素
D）信息项

四、写一算法，将一个带表头结点的单链表逆转。要求在原链表上进行，不允许重新构造一个表。

